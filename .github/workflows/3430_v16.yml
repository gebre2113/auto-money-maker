name: Profit Master Supreme - STRICT MODE

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  execute_real_work:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
      - name: "📥 Checkout Repository"
        uses: actions/checkout@v4

      - name: "🐍 Setup Python 3.12"
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: "📦 Install Dependencies"
        run: |
          pip install --upgrade pip
          pip install pandas numpy requests schedule plotly streamlit groq \
          google-generativeai openai anthropic cohere python-telegram-bot \
          beautifulsoup4 lxml Pillow gTTS moviepy yt-dlp sqlalchemy \
          streamlit-option-menu pydantic fastapi uvicorn nltk spacy textblob
          python -m nltk.downloader punkt stopwords

      - name: "🔍 Verify Main Script"
        run: |
          echo "Checking for main.py..."
          if [ ! -f "main.py" ]; then
            echo "❌ CRITICAL ERROR: 'main.py' not found in the repository!"
            echo "Please upload your Python script as 'main.py'."
            ls -la
            exit 1  # ይህ ስክሪፕቱን ያቆመዋል (Red Light)
          fi
          echo "✅ 'main.py' found successfully."

      - name: "🤖 Run Real Work"
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          echo "🚀 Executing Real Profit Master Script..."
          python3 -u main.py --auto

      # ... (የተቀረው የዳሽቦርድ እና የቴሌግራም ክፍል ተመሳሳይ ነው) ...
