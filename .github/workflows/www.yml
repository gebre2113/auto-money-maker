name: 👑 TITAN v22.0 ULTIMATE MULTI-CHANNEL ORACLE

on:
  # 1. በየ 6 ሰዓቱ በራስ-ሰር ይነሳል (ለቋሚ ገቢ ፍሰት)
  schedule:
    - cron: '0 */6 * * *'
      
  # 2. በማንኛውም ጊዜ በእጅ ለማስጀመር
  workflow_dispatch:
    inputs:
      topic:
        description: 'Target Topic (e.g., Global Finance 2026)'
        required: false
        default: 'Future of Global Wealth and AI 2026'
      language:
        description: 'Primary Language'
        required: false
        default: 'en'

# የሲስተም ፈቃዶች
permissions:
  contents: write
  issues: write
  actions: read

# ተደጋጋሚ ስራዎችን ለመከላከል (Concurrency)
concurrency:
  group: production-pipeline
  cancel-in-progress: true

jobs:
  run-sovereign-engine:
    name: 👑 Execute Ultimate Production & Distribution
    runs-on: ubuntu-latest
    timeout-minutes: 180 # ለረጅም ይዘት ሰፊ ጊዜ መስጠት

    steps:
    # 1. ኮዱን ማውረድ
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    # 2. ፓይተን ማዘጋጀት (Python 3.12 - Latest Stable)
    - name: 🐍 Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    # 3. የሲስተም ሶፍትዌሮች (ለቪዲዮ፣ ምስል እና ድምጽ ሂደት)
    - name: 🎥 Install System Tools
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg libmagick++-dev

    # 4. ፓይተን ላይብረሪዎች (ሁሉንም የላቁ የማህበራዊ ሚዲያ እና AI ሞጁሎችን ያካትታል)
    - name: 📦 Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install tenacity requests psutil python-dotenv pyyaml aiohttp asyncio \
                    google-generativeai groq rehabilitation openai \
                    beautifulsoup4 pandas jinja2 lxml \
                    textblob nltk numpy \
                    python-wordpress-xmlrpc facebook-sdk tweepy

        # የቋንቋ ማቀነባበሪያ ዳታዎችን ማውረድ
        python -m nltk.downloader punkt stopwords wordnet averaged_perceptron_tagger_eng
        
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    # 5. የሜጋ ምርት እና የስርጭት ሂደትን ማስጀመር
    - name: 👑 Run Unified Sovereign Engine
      env:
        # --- AI API KEYS (MULTI-KEY ROTATION) ---
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        GROQ_API_KEY_1: ${{ secrets.GROQ_API_KEY_1 }}
        GROQ_API_KEY_2: ${{ secrets.GROQ_API_KEY_2 }}
        GROQ_API_KEY_3: ${{ secrets.GROQ_API_KEY_3 }}
        GROQ_API_KEY_4: ${{ secrets.GROQ_API_KEY_4 }}
        GROQ_API_KEY_5: ${{ secrets.GROQ_API_KEY_5 }}
        GROQ_API_KEY_6: ${{ secrets.GROQ_API_KEY_6 }}
        GROQ_API_KEY_7: ${{ secrets.GROQ_API_KEY_7 }}
        
        # --- OTHER AI PROVIDERS ---
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        AI_CULTURAL_API_KEY: ${{ secrets.AI_CULTURAL_API_KEY }}
        AI_AUDIT_API_KEY: ${{ secrets.AI_AUDIT_API_KEY }}
        AI_TITLE_API_KEY: ${{ secrets.AI_TITLE_API_KEY }}

        # --- MULTI-CHANNEL DISTRIBUTION KEYS ---
        # WordPress
        WP_URL: ${{ secrets.WP_URL }}
        WP_USERNAME: ${{ secrets.WP_USERNAME }}
        WP_PASSWORD: ${{ secrets.WP_PASSWORD }}
        
        # Telegram
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        
        # Facebook
        FB_ACCESS_TOKEN: ${{ secrets.FB_ACCESS_TOKEN }}
        FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
        
        # Twitter (X)
        TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
        TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
        TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
        TWITTER_ACCESS_SECRET: ${{ secrets.TWITTER_ACCESS_SECRET }}
        
        # LinkedIn
        LINKEDIN_TOKEN: ${{ secrets.LINKEDIN_TOKEN }}
        LINKEDIN_URN: ${{ secrets.LINKEDIN_URN }}
        
        # --- CONFIGURATION ---
        ENTERPRISE_TOPIC: ${{ github.event.inputs.topic || 'Future of Global Wealth and AI 2026' }}
        INPUT_TOPIC: ${{ github.event.inputs.topic || 'Future of Global Wealth and AI 2026' }}
        GITHUB_ACTIONS: "true"
        PYTHONPATH: .
        PYTHONUNBUFFERED: 1
      run: |
        echo "🚀 Starting Unified Sovereign Engine..."
        echo "🌍 Target Topic: $INPUT_TOPIC"
        # ራነሩን ማስጀመር - የ 10 ቁጥር ምርጫን በራስ ሰር ይልካል
        python main.py <<EOF
        10
        $INPUT_TOPIC
        EOF

    # 6. የላቀ የዳሽቦርድ ሪፖርት (GitHub Issues)
    - name: 📊 Update GitHub Enterprise Dashboard
      if: success()
      run: |
        TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S')
        TOPIC="${{ github.event.inputs.topic || 'Automated Run' }}"
        
        BODY="## 👑 Sovereign Production Complete
        **Status:** ✅ Successfully Generated & Distributed
        **Topic:** $TOPIC
        **Execution Time:** $TIMESTAMP
        
        ### 📡 Distribution Channels:
        - [x] **WordPress:** Draft Created
        - [x] **Telegram:** Report Sent
        - [x] **Facebook:** Page Updated
        - [x] **Twitter/X:** Tweet Sent
        - [x] **LinkedIn:** Article Shared
        
        ### 📂 Artifacts:
        Full reports and JSON data are available in the 'Actions' tab under 'sovereign-production-v22'."

        curl -X POST \
          -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
          -H "Accept: application/vnd.github.v3+json" \
          -d "{\"title\": \"✅ Production: $TOPIC [$TIMESTAMP]\", \"body\": \"$BODY\"}" \
          "https://api.github.com/repos/${{ github.repository }}/issues"

    # 7. አዳዲስ ፋይሎችን ወደ ሪፖዚቶሪ መመለስ (Smart Rebase)
    - name: 📤 Sync & Push to GitHub
      if: always()
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "Sovereign-Bot 👑"
        
        # ሁሉንም የምርት ውጤቶች ማካተት
        mkdir -p enterprise_outputs production_backups enterprise_logs ai_usage_logs
        git add enterprise_outputs/ production_backups/ enterprise_logs/ ai_usage_logs/ *.html *.json *.md || true
        
        if git diff --staged --quiet; then
          echo "🚫 No changes to commit."
        else
          echo "✅ Changes detected. Pushing to repository..."
          git commit -m "👑 Sovereign Output: [Run ${{ github.run_number }}] - Update Content & Logs"
          git pull --rebase origin main
          git push origin main
        fi

    # 8. አርቲፋክቶችን ማስቀመጥ (ለማውረድ እንዲመች)
    - name: 💾 Archive Production Results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: sovereign-production-v22
        retention-days: 30
        path: |
          enterprise_outputs/
          enterprise_logs/
          production_backups/
          *.html
          *.json
