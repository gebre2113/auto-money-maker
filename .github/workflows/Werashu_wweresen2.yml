Name: 🚀 Profit Master Supreme - EXECUTE open

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      topic:
        description: 'የይዘት ርዕስ (Topic)'
        required: false
        default: 'AI Revolution 2026'
      language:
        description: 'ቋንቋ (am/en)'
        required: false
        default: 'am'

permissions:
  contents: write

jobs:
  run_profit_master:
    name: "🚀 Run Profit Master System"
    runs-on: ubuntu-latest
    
    steps:
      - name: "📥 Checkout Repository"
        uses: actions/checkout@v4

      - name: "🐍 Setup Python 3.12"
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: "🔧 Install System Dependencies"
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: "📦 Install Python Libraries"
        run: |
          python -m pip install --upgrade pip
          pip install \
            aiohttp httpx requests pandas numpy schedule \
            groq google-generativeai openai anthropic cohere \
            gtts moviepy>=2.0.0 pytube yt-dlp Pillow \
            beautifulsoup4 selenium tweepy \
            sqlalchemy redis fastapi uvicorn psutil \
            nltk spacy textblob pydantic==1.10.11 prometheus-client boto3 \
            deep-translator transformers
          
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          python -m nltk.downloader brown punkt punkt_tab stopwords wordnet averaged_perceptron_tagger_eng conll2000 movie_reviews
          python -m spacy download en_core_web_sm || true

      - name: "🤖 Execute Your Script"
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          INPUT_TOPIC: ${{ github.event.inputs.topic || 'AI Revolution 2026' }}
          INPUT_LANGUAGE: ${{ github.event.inputs.language || 'am' }}
          PYTHONUNBUFFERED: 1
        run: |
          echo "🚀 Starting automated execution..."
          
          # ፋይሉን በስማቸው ፈልጎ እንዲያስተካክልና እንዲያነሳ የሚያደርግ ሎጂክ
          if [ -f "fixer.py" ] && [ -f "enterprise_production_runner.py" ]; then
             echo "🔧 Running Fixer on enterprise_production_runner.py..."
             echo "enterprise_production_runner.py" | python fixer.py
             
             # ከተስተካከለ በኋላ FIXED የሚለውን ፋይል ያስነሳል
             if [ -f "enterprise_production_runner_FIXED.py" ]; then
                echo "🚀 Running the FIXED Elite Machine..."
                yes 1 | python enterprise_production_runner_FIXED.py
             else
                echo "⚠️ Fixer failed to create FIXED file, running original..."
                yes 1 | python enterprise_production_runner.py
             fi
          elif [ -f "main.py" ]; then
             echo "🚀 Running main.py..."
             yes 1 | python main.py
          else
             echo "❌ Error: Neither main.py nor enterprise_production_runner.py found!"
             ls -R # ያሉትን ፋይሎች ዝርዝር በሎግ ላይ ያሳያል (ለመለየት ይረዳል)
             exit 1
          fi

      - name: "💾 Upload Artifacts"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: execution-results
          path: |
            enterprise_logs/
            enterprise_outputs/
            profit_master.log
            crash_report.log
            *.json
            *.html
          retention-days: 5
