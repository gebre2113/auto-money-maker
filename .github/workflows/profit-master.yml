name: Profit Master Supreme v12.0 - Final

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  automation_run:
    runs-on: ubuntu-latest
    steps:
      - name: "📥 Checkout Code"
        uses: actions/checkout@v4

      - name: "🐍 Setup Python"
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: "📦 Install Packages"
        run: pip install requests pandas groq replicate python-dotenv

      - name: "🚀 Run Main Engine"
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          REPLICATE_API_TOKEN: ${{ secrets.REPLICATE_API_TOKEN }}
        run: |
          mkdir -p scripts
          # ፋይሉ መኖሩን እያረጋገጠ ያንቀሳቅሰዋል
          python3 scripts/main_engine.py
          
