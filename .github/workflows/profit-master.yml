name: Profit Master Supreme v12.0 - Final Fixed

on:
  schedule:
    # በየ30 ደቂቃው በራሱ እንዲነሳ
    - cron: '*/30 * * * *'
  # እኛ በራሳችን ሄደን ማስነሳት ስንፈልግ
  workflow_dispatch:

jobs:
  automation_run:
    runs-on: ubuntu-latest
    steps:
      - name: "📥 ኮድ ማውረድ (Checkout)"
        uses: actions/checkout@v4

      - name: "🐍 Python ማዘጋጀት"
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: "📦 አስፈላጊ ላይብረሪዎችን መጫን"
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas groq replicate python-dotenv schedule beautifulsoup4

      - name: "🚀 ዋናውን ኢንጂን ማስነሳት"
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          REPLICATE_API_TOKEN: ${{ secrets.REPLICATE_API_TOKEN }}
        run: |
          # የ scripts ፎልደር መኖሩን ያረጋግጣል
          mkdir -p scripts
          # ፋይሉ መኖሩን አረጋግጦ ያስነሳል
          if [ -f scripts/main_engine.py ]; then
            python3 scripts/main_engine.py
          else
            echo "❌ ስህተት: scripts/main_engine.py አልተገኘም!"
            exit 1
          fi
